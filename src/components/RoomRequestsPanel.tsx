import React from 'react';

// TODO adapted for new Supabase backend - room requests functionality disabled
const RoomRequestsPanel: React.FC = () => {
  return (
    <div className="p-4 text-center text-gray-500">
      <p>Room requests panel is temporarily disabled during backend migration.</p>
    </div>
  );
};

export default RoomRequestsPanel;
